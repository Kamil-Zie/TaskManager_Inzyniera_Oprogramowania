{
  "name": "project-runner",
  "version": "1.0.0",
  "description": "A script to run both the backend and frontend.",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd ./TaskManager_API && sudo dotnet build && sudo dotnet run\" \"cd ./taskmanager-webapp && sudo npm run start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
