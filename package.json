{
  "name": "project-runner",
  "version": "1.0.0",
  "description": "A script to run both the backend and frontend.",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd ./TaskManager_API && dotnet build && dotnet run\" \"cd ./taskmanager-webapp && npm run start\" \"cd ./TaskManager_API.Tests && dotnet build && dotnet test\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
